#include <iostream>
#include <string>

using namespace std;

int main()
{
    string word;
    int n;
    cin >> n;
    cin >> word;
    int dlugosc = word.length();
    if (n>dlugosc)
    {
        cout << word;
        exit(0);
    }
    string szukaj = word.find(szukaj)
    if (find.)
    {
        for (int i = 0; i<n; i++)
        {
            word = word[dlugosc-1];
        }
    }
}